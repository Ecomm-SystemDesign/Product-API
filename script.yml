config:
  target: "http://localhost:3000"
  phases:
    - duration: 10
      arrivalRate: 1000
scenarios:
  - name: "Stress testing"
    flow:
      - get:
          url: "/products/{{$randomNumber(20000,30000)}}/styles"
      - think: 1