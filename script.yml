config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: "Please work"
    flow:
      - get:
          url: "/products/{{$randomNumber(1000,10000)}}/styles"
      - think: 1