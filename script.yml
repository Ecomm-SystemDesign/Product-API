config:
  target: "http://localhost:3000"
  phases:
    - duration: 1
      arrivalRate: 1000
scenarios:
  - name: "Stress testing"
    flow:
      - get:
          url: "/products/{{$randomNumber(1,30000)}}/related"
      - think: 1